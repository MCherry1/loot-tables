# Warnings to Discuss with Agent

Running list of concerns, gotchas, and open questions that surfaced while
executing tasks on this branch. Each entry is something I think needs a
conversational deep-dive with a chat agent before a fix is committed —
either because the root cause is architectural, or because there are
multiple reasonable fixes and the trade-offs matter.

---

## 1. `src/data/magic-items.ts` hand edits can be clobbered by `npm run extract`

**Surfaced during:** Priority 0 — Data Cleanup (commit `4fc0912`).

**The situation:**
- `src/data/magic-items.ts` has an `// Auto-generated by scripts/extract-data.ts — do not edit` header.
- `TASKS.md` Priority 0 explicitly directed substantial hand edits into this file: three new subtables (`Elemental-Essence-Shard`, `Outer-Essence-Shard`, `Carpet-of-Flying`), rewriting Sorcerer-G parent entries, and ~50 item renames to 5etools canonical form.
- The generator source is `data/Item Tables.xlsx`. Re-running `npm run extract` will regenerate `magic-items.ts` from the Excel file and wipe every one of those hand edits.
- Meanwhile, the newer `npm run sync` pipeline (pulls from 5etools, updates curation + item-stats) does NOT touch `magic-items.ts` — so there are now two data-flow stories in the repo that disagree about what the source of truth is.

**Why this is a discussion, not a quick fix:**
There are at least four credible resolutions and each has trade-offs:

1. **Treat `magic-items.ts` as the source of truth going forward.** Delete `scripts/extract-data.ts` (or at least the magic-items path), drop the auto-gen header, and stop distributing `Item Tables.xlsx`. Clean but loses the Excel-as-design-tool workflow.
2. **Edit the Excel file instead.** Do the renames and subtable restructuring in `Item Tables.xlsx`, then re-run `extract`. Keeps the pipeline but requires re-doing all the hand edits in Excel, and Excel can't easily express subtables the same way TS can.
3. **Move the transformations into `extract-data.ts` as a post-processing pass.** Add a `rectify()` step after `readAllTables` that applies the renames and subtable restructuring programmatically. Idempotent across re-runs, but adds a second source of name mappings that must stay in sync with 5etools.
4. **Leave it alone and just don't run `npm run extract` anymore.** Pragmatic but fragile — a contributor running `extract` without reading the commit history will silently wipe the Priority 0 work.

**What I'd want to understand before fixing:**
- How often is `Item Tables.xlsx` actually updated? Is it still the design tool the author iterates in, or is it archaeology?
- Is the Excel file still useful for anything else (gems, art, hoards)?
- What is the long-term plan — fully migrate to the sync pipeline, or keep both?

Orphan curation entries are a related side effect of this same issue:
`data/curation.json` still has entries keyed to the old names (`"Air Essence Shard|TCE"`, `"Carpet of Flying|DMG"`, etc.). They're harmless (no match = no override) but they're stale. A proper migration should either update or delete them.

---
